# Selenium-HybridDemo
This a Selenium java automation project used for register and login a user in a shopping website. The tests are written in TestNG while maven , jenkins are used to run and test the results.
